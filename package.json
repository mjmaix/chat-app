{
  "private": true,
  "name": "ChatAndVideoApp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cleanup:all": "yarn cleanup:yarn && yarn cleanup:node && yarn && yarn common:dev:build && yarn web:dev:start",
    "cleanup:node": "rm -rf packages/*/node_modules && rm -rf node_modules",
    "cleanup:yarn": "rm -f packages/*/yarn.lock && rm -f yarn.lock",
    "common:dev:build": "cd ./packages/common && yarn build",
    "web:dev:start": "cd ./packages/web && yarn start"
  },
  "repository": {
    "type": "git",
    "url": "https://git-codecommit.ap-southeast-1.amazonaws.com/v1/repos/ChatAndVideoApp"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ]
}
